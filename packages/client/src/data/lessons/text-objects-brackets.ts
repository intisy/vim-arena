import type { Lesson } from '@/types/lesson'

export const TEXT_OBJECTS_BRACKETS_LESSONS: Lesson[] = [
  {
    id: 'intro-to-text-objects',
    categoryId: 'text-objects-brackets',
    title: 'Intro to Text Objects',
    description: 'Understand inside and around text objects',
    order: 1,
    type: 'theory',
    keyCards: [
      { key: 'i', description: 'inside — the content within delimiters' },
      { key: 'a', description: 'around — includes the delimiters themselves' },
    ],
    explanation: `Text objects let you operate on structured chunks of text — everything inside parentheses, a quoted string, or a word boundary. They follow the pattern: **operator + i/a + object**.

\`i\` means "inside" — it selects the content between delimiters but not the delimiters themselves. \`a\` means "around" — it includes the delimiters too.

For example, with the cursor anywhere inside \`(hello world)\`:
- \`di(\` deletes \`hello world\` but keeps the parentheses: \`()\`
- \`da(\` deletes the entire \`(hello world)\` including the parens

Text objects work with any operator: \`d\` (delete), \`c\` (change), \`y\` (yank). You can use \`(\`, \`## {\`, \`[\`, \`"\`, \`'\`, \`w\`, \`p\` and more as the object type.

In the next lessons, you will practice each combination.`,
    steps: [],
    prerequisiteIds: [],
  },
  {
    id: 'delete-inside-brackets',
    categoryId: 'text-objects-brackets',
    title: 'Delete Inside Brackets',
    description: 'Delete content inside brackets with di{ di( di[',
    order: 2,
    type: 'step-based',
    keyCards: [
      { key: 'di(', description: 'delete inside parentheses' },
      { key: 'di{', description: 'delete inside curly braces' },
      { key: 'di[', description: 'delete inside square brackets' },
    ],
    explanation: `\`di(\` deletes everything inside parentheses, leaving the empty parens. Works with \`{\`, \`[\`, and their closing counterparts. The cursor can be anywhere inside the brackets.`,
    steps: [
      {
        id: 'di-paren',
        instruction: 'Delete the arguments inside the parentheses using `di(`',
        initialContent: 'console.log("hello", "world");',
        initialCursor: { line: 0, column: 15 },
        expectedContent: 'console.log();',
        expectedCursor: undefined,
        hints: ['Press di( with cursor anywhere inside the parens'],
        requiredCommands: ['di('],
      },
      {
        id: 'di-curly',
        instruction: 'Delete the object properties inside the braces using `di{`',
        initialContent: 'const config = { port: 3000, host: "localhost" };',
        initialCursor: { line: 0, column: 20 },
        expectedContent: 'const config = {};',
        expectedCursor: undefined,
        hints: ['Press di{ with cursor inside the braces'],
        requiredCommands: ['di{'],
      },
      {
        id: 'di-square',
        instruction: 'Delete the array contents using `di[`',
        initialContent: 'const colors = ["red", "green", "blue"];',
        initialCursor: { line: 0, column: 20 },
        expectedContent: 'const colors = [];',
        expectedCursor: undefined,
        hints: ['Press di[ with cursor inside the brackets'],
        requiredCommands: ['di['],
      },
      {
        id: 'di-nested',
        instruction: 'Delete the inner function arguments using `di(`',
        initialContent: 'Math.max(Math.min(value, 100), 0);',
        initialCursor: { line: 0, column: 22 },
        expectedContent: 'Math.max(Math.min(), 0);',
        expectedCursor: undefined,
        hints: ['Place cursor inside the inner parens and press di('],
        requiredCommands: ['di('],
      },
      {
        id: 'di-function-body',
        instruction: 'Delete the function body inside the braces using `di{`',
        initialContent: 'function reset() { count = 0; active = false; }',
        initialCursor: { line: 0, column: 25 },
        expectedContent: 'function reset() {}',
        expectedCursor: undefined,
        hints: ['Press di{ with cursor inside the function body'],
        requiredCommands: ['di{'],
      },
    ],
    prerequisiteIds: ['intro-to-text-objects'],
  },
  {
    id: 'delete-around-brackets',
    categoryId: 'text-objects-brackets',
    title: 'Delete Around Brackets',
    description: 'Delete brackets and their content with da{ da( da[',
    order: 3,
    type: 'step-based',
    keyCards: [
      { key: 'da(', description: 'delete around parentheses (including parens)' },
      { key: 'da{', description: 'delete around curly braces' },
      { key: 'da[', description: 'delete around square brackets' },
    ],
    explanation: `\`da(\` deletes everything inside parentheses AND the parentheses themselves. Use "around" when you want to remove the entire bracketed expression.`,
    steps: [
      {
        id: 'da-paren',
        instruction: 'Delete the entire parenthesized expression using `da(`',
        initialContent: 'const result = calculate(price * quantity);',
        initialCursor: { line: 0, column: 30 },
        expectedContent: 'const result = calculate;',
        expectedCursor: undefined,
        hints: ['Press da( to delete the parens and everything inside'],
        requiredCommands: ['da('],
      },
      {
        id: 'da-curly',
        instruction: 'Delete the entire object literal using `da{`',
        initialContent: 'const opts = { timeout: 5000 };',
        initialCursor: { line: 0, column: 18 },
        expectedContent: 'const opts = ;',
        expectedCursor: undefined,
        hints: ['Press da{ to remove the braces and contents'],
        requiredCommands: ['da{'],
      },
      {
        id: 'da-square',
        instruction: 'Delete the entire array using `da[`',
        initialContent: 'const items = [1, 2, 3];',
        initialCursor: { line: 0, column: 16 },
        expectedContent: 'const items = ;',
        expectedCursor: undefined,
        hints: ['Press da[ to delete the brackets and contents'],
        requiredCommands: ['da['],
      },
      {
        id: 'da-method-call',
        instruction: 'Delete the entire argument list `(err)` using `da(`',
        initialContent: 'if (err) {\n  handleError(err);\n}',
        initialCursor: { line: 0, column: 5 },
        expectedContent: 'if  {\n  handleError(err);\n}',
        expectedCursor: undefined,
        hints: ['Press da( on the if condition'],
        requiredCommands: ['da('],
      },
    ],
    prerequisiteIds: ['delete-inside-brackets'],
  },
  {
    id: 'change-inside-brackets',
    categoryId: 'text-objects-brackets',
    title: 'Change Inside Brackets',
    description: 'Replace bracket contents with ci{ ci( ci[',
    order: 4,
    type: 'step-based',
    keyCards: [
      { key: 'ci(', description: 'change inside parentheses' },
      { key: 'ci{', description: 'change inside curly braces' },
      { key: 'ci[', description: 'change inside square brackets' },
    ],
    explanation: `\`ci(\` deletes the content inside parentheses and enters insert mode so you can type a replacement. The brackets are preserved. This is one of the most powerful Vim editing patterns.`,
    steps: [
      {
        id: 'ci-paren-args',
        instruction: 'Change the function arguments to `name, age` using `ci(`',
        initialContent: 'function createUser(email, password, role) {',
        initialCursor: { line: 0, column: 25 },
        expectedContent: 'function createUser(name, age) {',
        expectedCursor: undefined,
        hints: ['Press ci(, type "name, age", press Esc'],
        requiredCommands: ['ci('],
      },
      {
        id: 'ci-curly-body',
        instruction: 'Change the object to `{ active: true }` using `ci{`',
        initialContent: 'const state = { loading: false, error: null };',
        initialCursor: { line: 0, column: 20 },
        expectedContent: 'const state = { active: true };',
        expectedCursor: undefined,
        hints: ['Press ci{, type " active: true ", press Esc'],
        requiredCommands: ['ci{'],
      },
      {
        id: 'ci-square-items',
        instruction: 'Change the array items to `"a", "b"` using `ci[`',
        initialContent: 'const tags = ["important", "urgent", "critical"];',
        initialCursor: { line: 0, column: 20 },
        expectedContent: 'const tags = ["a", "b"];',
        expectedCursor: undefined,
        hints: ['Press ci[, type the new items, press Esc'],
        requiredCommands: ['ci['],
      },
      {
        id: 'ci-condition',
        instruction: 'Change the if condition to `isValid` using `ci(`',
        initialContent: 'if (count > 0 && active === true) {',
        initialCursor: { line: 0, column: 10 },
        expectedContent: 'if (isValid) {',
        expectedCursor: undefined,
        hints: ['Press ci(, type "isValid", press Esc'],
        requiredCommands: ['ci('],
      },
    ],
    prerequisiteIds: ['delete-around-brackets'],
  },
  {
    id: 'change-around-brackets',
    categoryId: 'text-objects-brackets',
    title: 'Change Around Brackets',
    description: 'Replace brackets and content with ca{ ca( ca[',
    order: 5,
    type: 'step-based',
    keyCards: [
      { key: 'ca(', description: 'change around parentheses' },
      { key: 'ca{', description: 'change around curly braces' },
      { key: 'ca[', description: 'change around square brackets' },
    ],
    explanation: `\`ca(\` deletes the brackets and their contents, then enters insert mode. Use this when you want to replace the entire bracketed expression including the delimiters.`,
    steps: [
      {
        id: 'ca-paren',
        instruction: 'Replace `(oldValue)` with `(newValue)` using `ca(`',
        initialContent: 'process(oldValue);',
        initialCursor: { line: 0, column: 10 },
        expectedContent: 'process(newValue);',
        expectedCursor: undefined,
        hints: ['Press ca(, type "(newValue)", press Esc'],
        requiredCommands: ['ca('],
      },
      {
        id: 'ca-curly',
        instruction: 'Replace the object `{ x: 1 }` with `{ y: 2 }` using `ca{`',
        initialContent: 'const point = { x: 1 };',
        initialCursor: { line: 0, column: 17 },
        expectedContent: 'const point = { y: 2 };',
        expectedCursor: undefined,
        hints: ['Press ca{, type "{ y: 2 }", press Esc'],
        requiredCommands: ['ca{'],
      },
      {
        id: 'ca-square',
        instruction: 'Replace `[0]` with `[index]` using `ca[`',
        initialContent: 'const first = items[0];',
        initialCursor: { line: 0, column: 20 },
        expectedContent: 'const first = items[index];',
        expectedCursor: undefined,
        hints: ['Press ca[, type "[index]", press Esc'],
        requiredCommands: ['ca['],
      },
    ],
    prerequisiteIds: ['change-inside-brackets'],
  },
  {
    id: 'brackets-review',
    categoryId: 'text-objects-brackets',
    title: 'Brackets Review',
    description: 'Practice all bracket text object operations',
    order: 6,
    type: 'step-based',
    keyCards: [
      { key: 'di(', description: 'delete inside parens' },
      { key: 'da{', description: 'delete around braces' },
      { key: 'ci[', description: 'change inside brackets' },
    ],
    explanation: `Put your bracket text object skills to the test. Mix and match \`d\`/\`c\` with \`i\`/\`a\` and bracket types.`,
    steps: [
      {
        id: 'review-di-paren',
        instruction: 'Delete the function arguments using `di(`',
        initialContent: 'addEventListener("click", handler, options);',
        initialCursor: { line: 0, column: 20 },
        expectedContent: 'addEventListener();',
        expectedCursor: undefined,
        hints: ['Press di('],
        requiredCommands: ['di('],
      },
      {
        id: 'review-ci-curly',
        instruction: 'Change the object to `{ done: true }` using `ci{`',
        initialContent: 'setState({ loading: true, error: null });',
        initialCursor: { line: 0, column: 15 },
        expectedContent: 'setState({ done: true });',
        expectedCursor: undefined,
        hints: ['Press ci{, type " done: true ", press Esc'],
        requiredCommands: ['ci{'],
      },
      {
        id: 'review-da-square',
        instruction: 'Delete the entire array expression using `da[`',
        initialContent: 'const subset = allItems[0, 1, 2];',
        initialCursor: { line: 0, column: 25 },
        expectedContent: 'const subset = allItems;',
        expectedCursor: undefined,
        hints: ['Press da['],
        requiredCommands: ['da['],
      },
    ],
    prerequisiteIds: ['change-around-brackets'],
  },
]
